#import * from 'types.graphql'

type Query{
    books: [Book]
    movies: [Movie]
    tvShows: [TVShow]
    articles: [Article]
    tags: [Tag]
}

type Mutation {
    authGoogle(input: AuthInput!): AuthResponse
    addBook: Book
}

schema{
    query: Query
    mutation: Mutation
}