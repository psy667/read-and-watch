# Migration `20200506001825-init`

This migration has been generated by psy667 at 5/6/2020, 12:18:25 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "Status" AS ENUM ('NOT_STARTED', 'STARTED', 'COMPLETED');

CREATE TABLE "public"."User" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text  NOT NULL ,"id" SERIAL,"name" text  NOT NULL ,"premium" boolean  NOT NULL DEFAULT false,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Book" (
"author" text   ,"comment" text   ,"currentPage" integer  NOT NULL DEFAULT 0,"description" text   ,"endDate" timestamp(3)   ,"groupId" integer   ,"id" SERIAL,"poster" text   ,"rating" Decimal(65,30)  NOT NULL DEFAULT 0.0,"startDate" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"status" "Status" NOT NULL DEFAULT 'NOT_STARTED',"title" text  NOT NULL ,"userId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Movie" (
"comment" text   ,"description" text   ,"director" text   ,"endDate" timestamp(3)   ,"groupId" integer   ,"id" SERIAL,"poster" text   ,"rating" Decimal(65,30)  NOT NULL DEFAULT 0.0,"startDate" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"status" "Status" NOT NULL DEFAULT 'NOT_STARTED',"title" text  NOT NULL ,"userId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."TVShow" (
"comment" text   ,"currentEpisode" integer  NOT NULL DEFAULT 0,"currentSeason" integer  NOT NULL DEFAULT 0,"description" text   ,"director" text   ,"endDate" timestamp(3)   ,"groupId" integer   ,"id" SERIAL,"poster" text   ,"rating" Decimal(65,30)  NOT NULL DEFAULT 0.0,"startDate" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"status" "Status" NOT NULL DEFAULT 'NOT_STARTED',"title" text  NOT NULL ,"userId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Article" (
"comment" text   ,"description" text   ,"endDate" timestamp(3)   ,"groupId" integer   ,"id" SERIAL,"link" text  NOT NULL ,"poster" text   ,"rating" Decimal(65,30)  NOT NULL DEFAULT 0.0,"startDate" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"status" "Status" NOT NULL DEFAULT 'NOT_STARTED',"title" text  NOT NULL ,"userId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Tag" (
"id" SERIAL,"title" text  NOT NULL ,"userId" integer   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Group" (
"id" SERIAL,"title" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."_BookToTag" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE TABLE "public"."_MovieToTag" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE TABLE "public"."_TVShowToTag" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE TABLE "public"."_ArticleToTag" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "_BookToTag_AB_unique" ON "public"."_BookToTag"("A","B")

CREATE  INDEX "_BookToTag_B_index" ON "public"."_BookToTag"("B")

CREATE UNIQUE INDEX "_MovieToTag_AB_unique" ON "public"."_MovieToTag"("A","B")

CREATE  INDEX "_MovieToTag_B_index" ON "public"."_MovieToTag"("B")

CREATE UNIQUE INDEX "_TVShowToTag_AB_unique" ON "public"."_TVShowToTag"("A","B")

CREATE  INDEX "_TVShowToTag_B_index" ON "public"."_TVShowToTag"("B")

CREATE UNIQUE INDEX "_ArticleToTag_AB_unique" ON "public"."_ArticleToTag"("A","B")

CREATE  INDEX "_ArticleToTag_B_index" ON "public"."_ArticleToTag"("B")

ALTER TABLE "public"."Book" ADD FOREIGN KEY ("groupId")REFERENCES "public"."Group"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Book" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Movie" ADD FOREIGN KEY ("groupId")REFERENCES "public"."Group"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Movie" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."TVShow" ADD FOREIGN KEY ("groupId")REFERENCES "public"."Group"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."TVShow" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Article" ADD FOREIGN KEY ("groupId")REFERENCES "public"."Group"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Article" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Tag" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."_BookToTag" ADD FOREIGN KEY ("A")REFERENCES "public"."Book"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_BookToTag" ADD FOREIGN KEY ("B")REFERENCES "public"."Tag"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_MovieToTag" ADD FOREIGN KEY ("A")REFERENCES "public"."Movie"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_MovieToTag" ADD FOREIGN KEY ("B")REFERENCES "public"."Tag"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_TVShowToTag" ADD FOREIGN KEY ("A")REFERENCES "public"."TVShow"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_TVShowToTag" ADD FOREIGN KEY ("B")REFERENCES "public"."Tag"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_ArticleToTag" ADD FOREIGN KEY ("A")REFERENCES "public"."Article"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_ArticleToTag" ADD FOREIGN KEY ("B")REFERENCES "public"."Tag"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200506001825-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,127 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+	provider = "postgresql"
+	url      = env("DATABASE_URL")
+}
+
+generator client {
+	provider = "prisma-client-js"
+}
+
+model User {
+	id          Int         @id @default(autoincrement())
+    email       String      @unique
+    name        String
+    premium     Boolean     @default(false)
+    books       Book[]
+    movies      Movie[]
+    tvShows     TVShow[]
+    articles    Article[]
+    tags        Tag[]
+    createdAt   DateTime    @default(now())
+}
+
+model Book{
+    id          Int         @id @default(autoincrement())
+    title       String      
+    description String?
+    status      Status      @default(NOT_STARTED)
+    startDate   DateTime    @default(now())
+    endDate     DateTime?
+    rating      Float       @default(0.0)    
+    comment     String?
+    poster      String?
+    tags        Tag[]       @relation(references: [id])    
+    groupId     Int?       
+    group       Group?      @relation(fields: [groupId], references: [id])
+    userId      Int     
+    user        User        @relation(fields: [userId], references: [id])
+
+    author      String?
+    currentPage Int         @default(0)
+}
+
+model Movie{
+    id          Int         @id @default(autoincrement())
+    title       String    
+    description String?  
+    status      Status      @default(NOT_STARTED)
+    startDate   DateTime    @default(now())
+    endDate     DateTime?
+    rating      Float       @default(0.0)    
+    comment     String?
+    poster      String?
+    tags        Tag[]       @relation(references: [id])    
+    groupId     Int?       
+    group       Group?      @relation(fields: [groupId], references: [id])
+    userId      Int     
+    user        User        @relation(fields: [userId], references: [id])
+
+    director    String?
+}
+
+model TVShow{
+    id          Int         @id @default(autoincrement())
+    title       String      
+    description String?
+    status      Status      @default(NOT_STARTED)
+    startDate   DateTime    @default(now())
+    endDate     DateTime?
+    rating      Float       @default(0.0)    
+    comment     String?
+    poster      String?
+    tags        Tag[]       @relation(references: [id])    
+    groupId     Int?       
+    group       Group?      @relation(fields: [groupId], references: [id])
+    userId      Int     
+    user        User        @relation(fields: [userId], references: [id])
+
+    director        String?
+    currentEpisode  Int     @default(0)
+    currentSeason   Int     @default(0)
+}
+
+model Article{
+    id          Int         @id @default(autoincrement())
+    title       String      
+    description String?
+    status      Status      @default(NOT_STARTED)
+    startDate   DateTime    @default(now())
+    endDate     DateTime?
+    rating      Float       @default(0.0)    
+    comment     String?
+    poster      String?
+    tags        Tag[]       @relation(references: [id])    
+    groupId     Int?       
+    group       Group?      @relation(fields: [groupId], references: [id])
+    userId      Int     
+    user        User        @relation(fields: [userId], references: [id])
+
+    link        String
+}
+
+model Tag{
+    id          Int         @id @default(autoincrement())
+    title       String      
+    books       Book[]      @relation(references: [id])
+    movies      Movie[]     @relation(references: [id])
+    tvShows     TVShow[]    @relation(references: [id])
+    articles    Article[]   @relation(references: [id])
+}
+
+model Group{
+    id          Int     @id @default(autoincrement())
+    title       String      
+    books       Book[]      
+    movies      Movie[]     
+    tvShows     TVShow[]    
+    articles    Article[]
+}
+
+enum Status {
+    NOT_STARTED
+    STARTED
+    COMPLETED
+}
```


